spring:
  datasource:
    type: com.alibaba.druid.pool.DruidDataSource
    driver-class-name: com.mysql.jdbc.Driver
    druid:
      master:
        url: jdbc:mysql://192.168.1.242:3306/small_neighborhood?useSSL=true&useUnicode=true&characterEncoding=utf-8&allowMultiQueries=true
        username: testuser
        password: Ceshi#@!0714
      slave:
        url: jdbc:mysql://192.168.1.242:3306/small_neighborhood?useSSL=true&useUnicode=true&characterEncoding=utf-8&allowMultiQueries=true
        username: testuser
        password: Ceshi#@!0714
      initial-size: 10
      max-active: 100
      min-idle: 10
      max-wait: 60000
      pool-prepareed-statements: true
      max-pool-prepared-statement-per-connection-size: 20
      time-between-eviction-runs-millis: 60000
      min-evictable-idle-time-millis: 30000
      validation-query: SELECT 1 FROM DUAL
      test-while-idle: true
      test-on-borrow: false
      test-on-return: false
      stat-view-servlet:
        enabled: true
        url-pattern: /druid/*
      filter:
        stat:
          log-show-sql: true
          show-sql-millis: 1000
          merge-sql: true
        wall:
          config:
            multi-statement-allow: true